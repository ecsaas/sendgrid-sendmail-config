# sendgrid-sendmail-config

```shell
 git init
 git branch -m main

 git config user.name manhbv.giaminh
 git config user.email manhbv.giaminh@gmail.com
 git add .
 git commit -m 'sendgrid-sendmail-config'

 git remote add origin git@github.com:ecsaas/sendgrid-sendmail-config.git
 git push origin main
```
